name: Type Check

on:
  push:
    branches: [master]
  pull_request:

jobs:
  mypy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run type check in container
        run: |
          docker compose build typing
          docker compose run --rm typing